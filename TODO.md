# 此文件为待办备忘以及agent提示词模板, 不必作为上下文参考.

# TODO:
## monarch:


## torrid:
基于immich http的二次开发:
https://api.immich.app/endpoints/api-keys
http://localhost:2283/doc
http://localhost:2283/api/spec.json
treasure页:
与monarch联动, 以后引入photoprism, 根据tags或文本识别内容检索图片, 类似小米相册plus.
对于图片类的提供拖拽四边裁切功能, (将相关数据发送至monarch让gallery执行).

gallery模块:
对于图片, 提供裁切/涂鸦. (后端做好支持, 也许需要重构通信json结构之类的).
对于视频, 提供简单的去头去尾的剪辑, 以及选取开始帧结束帧生成新小视频片段.
添加图片旋转功能, 改变图片的方向.

## Desktop App:
-- 漫画连载追踪页面.
可以根据设定的source字段(漫画网站的该漫画详细页url)选择某一预输入的爬虫脚本(python/或者打包的exe).增量更新. 并在安卓端如果已下载该漫画可以点击增量更新下载.(monarch->torrid).


## 数据库:



请帮我完成以下跨项目改动：
(⭐后续android端也加上字段updated_at?以便备份时正确备份修改后的. (又或者不用? 待考虑))
1. android端和backend端的数据备份和同步部分中, 对于booklet和essay两部分数据备份到服务器时, 目前是直接存储为json文件并将图片保存到"./static/img_storage/[模块名]/"下,为了后续能更容易实现文章的关键词模糊查询和内容以及图片文件的同步/备份时的增量更新之类的, 我希望把这两部分简单的json存储方式替换为数据表存储, 图片文件保持现有逻辑.
关于这两模块的数据表我已建好,表结构参考AGENTS_DB.md中的'用户数据表'部分. (所在scheme名为user_data)
(是否推荐android端也加上字段updated_at?以便备份时正确备份修改后的. 或者不变以保持简单?)
另外:
android端的某篇随笔重写/删除后是沿用原先id字段值还是新的?对于这些情况, 考虑好数据往来时正确传输完整且正确的数据. (也许省事简单的选择是json数据每次都完整传输, 而图片文件按需传输?)
backend端接收备份数据时, 若这两部分有图片不被包含在最新的数据中则删除对应文件.
2. desktop: 
日志页内容改为'不可编辑但可以手动复制的文本区', 以便我手动复制需要的部分. 并且随着日志内容的增加, 自动处于最末尾(呈现最新的部分).
android:
漫画模块, 对于漫画的详细页appbar添加一个action按钮, 切换该漫画的"readed"字段, 表示该漫画已看完.
漫画的总览页appbar的action按钮呼出的菜单加入一个"同步状态", 将本地标记为readed的漫画信息发送给后端服务器, 并检查所有已存于本地的漫画在服务器是否有更多新章节, 如果有则自动加入下载队列开始增量更新.  (当前ui不符, 我希望都出现在同一个action按钮呼出的菜单中).

完成所有工作之后进行检验,务必确保项目不含报错并且实现我提到的所有功能.



--------------
zx1360.github.io
问ds, 也许加点网页功能.



# 提示词模板:

[改动描述]
请帮我完成以下跨项目改动：
1. backend: 
2. android: 

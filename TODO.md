# 此文件为待办备忘以及agent提示词模板, 不必作为上下文参考.

# TODO:
现在我pc部署了immich容器(通过external library已将当前后端同一批媒体文件引入完成). 
1. 希望能结合immich容器本身的ai的orc图片文本识别, 人脸分类, 场景描述, 关键词搜索等等之类的功能与android端当前的gallery模块结合. (比如能否实现让immich将常见分类的媒体标签自动识别然后写入我的数据库的对应字段值中, (当然最好能区别一下标签是immich打的还是我手动设置的), 又比如我在应用中输入"可爱的女生"或者"梗图/meme图"后, immich能开始检索并返回所有符合文件并提供一键为返回的文件在数据库的对应字段加上该标签的功能.)
2. 另外在android端的library模块尽可能地复刻immich官方安卓app地各种功能包括ui/交互等, (不过这一步可以稍微先做个大概, 不必立马做到面面俱到.)

(感觉实现上有点难度, 因为immich运行于windows的docker容器中, 然后Go的http服务器反向代理发往immich的请求, 也许存在immich容器访问pc上的数据库的困难?)
(在我当前自用情况下图简单, 连接到immich时认证用的xapikey就让后端统一对转发至immich的请求头的"X-API-Key"进行替换, 我已将该值写入.env文件, 可通过"os.Getenv("API_KEY_IMMICH")"获取. )
为我提供建议.

----
现在我pc部署了immich容器(通过external library已将当前后端同一批媒体文件引入完成). 
在android端的library模块尽可能地复刻immich官方安卓app的各种页面功能包括ui/交互等, (即将原app的各页面都整合到当前应用的该模块中, 不过这一步可以稍微先做个大概, 不必立马做到面面俱到.)


(在我当前自用情况下简单起见, 连接到immich时认证用的xapikey就让后端统一对转发至immich的请求头的"X-API-Key"进行替换, 我已将该值写入.env文件, 可通过"os.Getenv("API_KEY_IMMICH")"获取. )

## monarch:

## torrid:
essay模块优化升级拓展? 或者另起一个模块用以更好体验的游评/影评, 理财学习, 文笔练习等等应用. (注重桌面端的书写, 这点再做考虑.)

gallery模块:
现需对android端的gallery模块做优化:
1. #sym:MediasGridViewPage 右边添加一个上下拖拽按钮, 能快速在当前的媒体列表上下滑动. 就像手机的相册应用都有的那样.
2. #sym:GallerySettingPage 组件呈现稍微密集点, 当前有些空旷. 另外增加一个数据总览组件卡片, 呈现当前服务端的总媒体文件数/图片占比和视频类等占比/sync_count同步次数字段情况, 标签相关情况等等之类的
然后对于媒体文件拉取时提供更多样的选择. 比如除了当前'首先按sync_count同步次数升序再按captured_at升序'以外, 提供可选的指定某类文件格式/按文件大小排序/特选某一年或月或日的等等. 并可选是否在默认的排序方式之后的结果的基础上二次排序.
(后端一并做好相应的接口).


👇(待验证)
对于视频, 提供简单的去头去尾的剪辑, 以及选取开始帧结束帧生成新小视频片段.
更多样的媒体文件队列(文件类型, 同步次数, 从某一时刻往后的正常规则, 文件大小等).

## Desktop App:
仪表盘页的运维信息优化.

-- 漫画连载追踪页面.(⭐待考虑, 慎重引入)
可以根据设定的source字段(漫画网站的该漫画详细页url)选择某一预输入的爬虫脚本(python/或者打包的exe).增量更新. 并在安卓端如果已下载该漫画可以点击增量更新下载.(monarch->torrid).



## 数据库:


--------------


# 提示词模板:

[改动描述]
请帮我完成以下跨项目改动:
1. backend: 
2. android: 
